1) Installation:
	Start by installing postgreSQL + security plugin
	Next, create tables, and load stored procedures into postgreSQL
2) There are two batch files: compile.bat and run.bat that are for use from command line.
3) Some libraries being used in this starter code are to be replaced by subsitute mentioned in project handout.
4) Commands are coded inside disptacher. Typicallty, they are placed in their own directory and each command
in separate file. You should do that as well.
5) run the Javascript tester to see input/output json.



